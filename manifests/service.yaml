apiVersion: v1
kind: Service
metadata:
  name: testworkflow
  namespace: namespace-workflow-1713849815789
spec:
  type: LoadBalancer
  selector:
    app: testworkflow
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000